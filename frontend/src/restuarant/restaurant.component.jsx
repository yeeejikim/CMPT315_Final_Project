import React from "react";

export const Restuarant = ({restuarant}) => {
    const{id,name} = restuarant;
    return(
        <div className="rest-container">
            <h2>name</h2>
        </div>
    )
};
